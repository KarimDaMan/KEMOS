// Public configuration only. Never put OAuth client secrets in this file.
window.KEMOS_AUTH = {
  // Create a Supabase project, enable Google and GitHub under Authentication
  // > Providers, then paste the public project URL and anon key below.
  supabaseUrl: "",
  supabaseAnonKey: ""
};
